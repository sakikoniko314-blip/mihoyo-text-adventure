## 2026-05-27

- 首次访问弹窗：简要介绍项目、免费额度规则、Key 获取方式，勾选可不再显示
- 标题"银河"统一改为"崩坏"
- 叙事输出限制 2-3 段、每段不超 5 句、整体 300 字内
- 修复删除主题功能后 `setWorldTheme` 残留调用导致 JS 报错选项丢失

## 2026-05-26

- 多用户隔离：全局 `current_game` 改为 `games[visitor_id]` 字典，每 IP 独立实例
- 流式输出覆盖自有 Key 用户：新增 `/api/adventure/stream-narrative` 端点
- 管理后台升级：统计卡片（启动数/回合数/活跃数）、配额重置按钮
- 叙事去重：以每段前 80 字为指纹自动跳过重复段落
- 手动结局：选项栏常驻"结束冒险"按钮
- 选项多样性：不再固定四类套路，要求至少一个意外选项
- 选项长度限制 15 字
- 叙事 prompt 调整：原神必须派蒙、感官优先
- 流式输出上线（免费层）
- 双 AI 架构正式拆分叙事与选项
- 结局系统：AI 判断自然终点 + 终章叙事
- 域名永久固定：命名 Tunnel 绑定 Cloudflare
